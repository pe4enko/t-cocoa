services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    image: t-cocoa-bot:local
    container_name: t-cocoa-bot
    restart: unless-stopped
    env_file:
      - .env
    init: true
